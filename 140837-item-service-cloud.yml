
spring:  
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect 
        
eureka:
  instance:
    nonSecurePort: 80        

    
item:
 create:
  exception:
     message: Item could not be added
  
